apiVersion: v1
kind: Service
metadata:
  name: instalock-cron
  namespace: production
spec:
  selector:
    app: instalock-cron
  ports:
    - name: http-cron
      protocol: TCP
      port: 80
      targetPort: 3051
