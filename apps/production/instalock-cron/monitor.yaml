apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: instalock-cron
  namespace: production
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: instalock-cron
  endpoints:
    - port: http-cron
      path: /api/metrics
      interval: 30s
      basicAuth:
        username:
          name: instalock-cron-production-secrets
          key: PROMETHEUS_USERNAME
        password:
          name: instalock-cron-production-secrets
          key: PROMETHEUS_PASSWORD
