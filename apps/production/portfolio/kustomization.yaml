apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - secrets.yaml
  - service.yaml
  # - monitor.yaml TODO: enable metrics in portfolio
commonLabels:
  app: portfolio
  environment: production
images:
  - name: tahminator/portfolio
    newTag: 1.0.1
