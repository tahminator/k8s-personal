apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: middleware-production
  namespace: flux-system
spec:
  interval: 5m
  path: ./base/production/middleware
  dependsOn:
    - name: traefik-infrastructure
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: production
