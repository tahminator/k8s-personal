apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ntfy-infrastructure
  namespace: flux-system
spec:
  interval: 10m
  path: ./base/infrastructure/ntfy
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: infrastructure
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
