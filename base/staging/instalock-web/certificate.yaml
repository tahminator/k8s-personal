apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-instalock-web-tls
  namespace: staging
spec:
  secretName: staging-instalock-web-tls
  dnsNames:
    - staging.instalock.app
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
