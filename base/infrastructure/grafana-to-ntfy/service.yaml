apiVersion: v1
kind: Service
metadata:
  name: grafana-to-ntfy
  namespace: production
spec:
  selector:
    app: grafana-to-ntfy
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
