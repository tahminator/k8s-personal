apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: infrastructure
spec:
  selector:
    cnpg.io/cluster: postgres
    cnpg.io/instanceRole: primary
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
