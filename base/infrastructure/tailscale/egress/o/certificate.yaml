apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: o-tls
  namespace: infrastructure
spec:
  secretName: o-tls
  dnsNames:
    - o.tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
