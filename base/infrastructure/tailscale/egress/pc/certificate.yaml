apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pc-tls
  namespace: infrastructure
spec:
  secretName: pc-tls
  dnsNames:
    - pc.tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
