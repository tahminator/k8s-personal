apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
  namespace: infrastructure
spec:
  secretName: vault-tls
  dnsNames:
    - vault.tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
