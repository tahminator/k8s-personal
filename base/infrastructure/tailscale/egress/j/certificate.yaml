apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: j-tls
  namespace: infrastructure
spec:
  secretName: j-tls
  dnsNames:
    - j.tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
