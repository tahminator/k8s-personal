apiVersion: v1
kind: Service
metadata:
  name: excalidash-frontend
  namespace: production
spec:
  selector:
    app: excalidash-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
