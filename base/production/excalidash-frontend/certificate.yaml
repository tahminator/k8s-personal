apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: production-excalidash-frontend-tls
  namespace: production
spec:
  secretName: production-excalidash-frontend-tls
  dnsNames:
    - draw.tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
