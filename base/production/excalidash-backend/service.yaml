apiVersion: v1
kind: Service
metadata:
  name: excalidash-backend
  namespace: production
spec:
  selector:
    app: excalidash-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
