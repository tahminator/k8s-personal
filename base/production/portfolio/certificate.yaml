apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: production-portfolio-tls
  namespace: production
spec:
  secretName: production-portfolio-tls
  dnsNames:
    - tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
