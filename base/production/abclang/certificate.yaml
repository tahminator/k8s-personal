apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: abclang-tls
  namespace: production
spec:
  secretName: abclang-tls
  dnsNames:
    - abclang.tahmid.io
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
