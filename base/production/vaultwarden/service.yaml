apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: production
spec:
  selector:
    app: vaultwarden
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
